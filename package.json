{
  "name": "gift-gabble-rec-lambda",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "nodemon ./local-api/index.mjs",
    "build": "rm -rf .dist && rm -rf .upload && esbuild ./lambda-wrapper/* --entry-names=[dir]/[name] --bundle --minify --sourcemap --platform=node --target=node20 --outdir=.dist && node utilities/zipDist.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.38.3",
    "request-promise": "^4.2.6",
    "zip-a-folder": "^3.1.6"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
