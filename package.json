{
  "name": "gift-gabble-rec-lambda",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "node ./local-api/index.mjs",
    "build": "rm -rf .dist && esbuild ./lambda-wrapper/* --entry-names=[dir]/[name] --bundle --minify --sourcemap --platform=node --target=node20 --outdir=.dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.38.3"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
